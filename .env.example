# Required — Supabase service-role connection (same DB as the main app)
SUPABASE_URL="https://<project>.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="eyJ..."

# Required — Groq for content generation
GROQ_API_KEY="gsk_..."

# Optional — log verbosity: 'debug' | 'info' | 'warn' | 'error' (default: info)
LOG_LEVEL="info"
